# Planned endpoint integration (M3/M4); M0 reads no environment credentials.
MODALMETER_API_KEY=
# Supply endpoint values locally when endpoint support exists.
MODALMETER_BASE_URL=
MODALMETER_METRICS_URL=
