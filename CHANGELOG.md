# Changelog

## Unreleased

- Make help-content tests independent of terminal ANSI styling, with explicit
  plain and forced-color cases covering the first hosted CI failure.
- Implement CPU image/video inspection with the pinned Qwen3-VL processor, exact
  eight-file immutable artifact cache, offline mode, typed API and atomic CLI runs.
- Record actual patch rows, visual placeholder IDs, attention-mask prompt length,
  grid-derived checks, source PTS, native timestamp labels and temporal padding.
- Bound file/decode/tensor workloads; reject corrupt metadata, unsupported geometry,
  reserved prompt markers and unsafe output replacement.
- Add portable Jinja2 reports, opt-in thumbnails, sanitized exports and input
  comparisons that expose coupled frame/resize effects without performance claims.
- Add independent CPU reference cases, original MIT PNG/FFV1 fixtures, base-only
  stored-report package checks and real M1/M2 example/evidence artifacts.

- Add the maintainer-supplied ModalMeter logo to the README.
- Preserve the owner-supplied implementation contract and existing MIT license.
- Establish product, architecture, measurement/data contracts, roadmap, contributor
  and agent instructions, testing/security/release guidance, and GitHub templates.
- Bootstrap the typed `src/` package, Typer help/version CLI, Hatchling artifacts,
  uv lock, Ruff, mypy, offline evidence validation tests, and CPU CI configuration.
- Add clean wheel/sdist base-install smoke checks and a labeled, weight-free
  Qwen3-VL processor/PyAV feasibility probe with source and artifact provenance.
- Record the metadata/tensor incompatibility and index/FPS timestamp limitations
  of the selected Transformers path in M0. ADR 0003 resolves metadata retention
  with a single call for M1/M2; endpoint benchmarking remains unimplemented.
